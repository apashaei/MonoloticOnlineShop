@page
@model Admin.EndPoint.Pages.CatalogType.EditModel
@{
}

<div class="statbox widget box box-shadow">
    <div class="widget-header">
        <div class="row">
            <div class="col-xl-12 col-md-12 col-sm-12 col-12">
                <h4>ویرایش دسته بندی </h4>
            </div>
        </div>
    </div>

    <div class="widget-content widget-content-area">
        @foreach(var item in Model.Messages)
        {
            <div class="alert alert-info">
                <p>@item</p>
                </div>
        }
    </div>
    <div class="widget-content widget-content-area">
        <form asp-page="edit" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-4">
                <input type="text" class="form-control" id="rEmail" asp-for="catalogTypeViewModel.Type">
                <span asp-validation-for="catalogTypeViewModel.Type" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="catalogTypeViewModel.Id" />
            

            <input type="submit" value="ثبت" class="btn btn-primary" />
            <a asp-page="index" class="btn btn-info">برگشت به لیست</a>
        </form>

    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
